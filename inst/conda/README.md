# Conda Environment Config

`SIDISH_*_environment.yml` were deprecated in SigBridgeR v3.8.1 due to R binding updates, left here for reference.

`celltypist_environment.yml` was deprecated in SigBridgeR v4.0.0, left here for reference. 
% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/49-CacheSysCall.R
\name{CacheSysCall}
\alias{CacheSysCall}
\title{System-Level Cache Load and Save}
\usage{
CacheSysCall(
  mode = c("load", "save"),
  path = if (mode == "save") cache@cache_path else NULL,
  cache,
  verbose = TRUE,
  timestamp = NULL,
  ...
)
}
\arguments{
\item{mode}{\code{character}. Either \code{"load"} or \code{"save"}.}

\item{path}{\code{character} or \code{NULL}. User-specified path. In save mode,
defaults to \code{cache@cache_path}. In load mode, defaults to \code{NULL} (user
will be prompted to choose).}

\item{cache}{A \code{ScreenMethodConfig} or \code{ScreenMethodCache} S7 object.
For save mode, a \code{ScreenMethodCache} is required (contains the data to
write). For load mode, either class is accepted.}

\item{verbose}{\code{logical}. Whether to print progress messages.
Default: \code{TRUE}.}

\item{timestamp}{\code{character} or \code{NULL}. Optional timestamp string for the
new cache directory in save mode. Passed to \code{\link[=CacheSetHere]{CacheSetHere()}}.}

\item{...}{Additional arguments passed to \code{\link[=WriteCache]{WriteCache()}} in save mode
(e.g., \code{format}, \code{max_rows}, \code{max_cols}).}
}
\value{
In \code{"load"} mode, a named list of loaded cache data objects. In
\code{"save"} mode, the cache data invisibly.
}
\description{
High-level wrapper for loading and saving cached screening results.
Combines cache path resolution (via \code{\link[=CacheSetHere]{CacheSetHere()}}), configuration
validation (via \code{\link[=CheckCache]{CheckCache()}}), and data I/O (via \code{\link[=LoadCache]{LoadCache()}} or
\code{\link[=WriteCache]{WriteCache()}}) into a single call.
}
\details{
\code{CacheSysCall} is the primary entry point for cache operations in the
SigBridgeR pipeline. It dispatches to either \code{CacheSysCall.load()} or
\code{CacheSysCall.save()} based on the \code{mode} argument.
\subsection{Load mode}{

In \code{"load"} mode, the function:
\enumerate{
\item Resolves the cache path via \code{\link[=CacheSetHere]{CacheSetHere()}}.
\item Validates cache consistency via \code{\link[=CheckCache]{CheckCache()}}.
\item Loads each cached data object via \code{\link[=LoadCache]{LoadCache()}} and returns them
as a named list.
}
}

\subsection{Save mode}{

In \code{"save"} mode, the function:
\enumerate{
\item Resolves and creates the cache directory via \code{\link[=CacheSetHere]{CacheSetHere()}}.
\item Writes the cache data and metadata via \code{\link[=WriteCache]{WriteCache()}}.
\item Returns the cache data invisibly.
}
}
}
\examples{
\dontrun{
# Load cached results
cache <- ScreenMethodCache(
  cache_path = "Scissor_res/",
  cache_config_path = "Scissor_res/cache_config.json",
  cache_data = list(),
  screen_method_config = ScreenMethodConfig(
    screen_method = "Scissor",
    param = list(alpha = 0.05)
  )
)
result <- CacheSysCall("load", cache = cache)

# Save results to cache
cache <- ScreenMethodCache(
  cache_path = "Scissor_res/",
  cache_config_path = "Scissor_res/cache_config.json",
  cache_data = list(result = my_matrix),
  screen_method_config = ScreenMethodConfig(
    screen_method = "Scissor",
    param = list(alpha = 0.05)
  )
)
CacheSysCall("save", cache = cache)
}
}
\seealso{
Other cache_config:
\code{\link[=CacheSetHere]{CacheSetHere()}},
\code{\link[=CheckCache]{CheckCache()}},
\code{\link[=ChooseCache]{ChooseCache()}},
\code{\link[=LoadCache]{LoadCache()}},
\code{\link[=WriteCache]{WriteCache()}},
\code{\link[=WriteCacheMeta]{WriteCacheMeta()}}
}
\concept{cache_config}

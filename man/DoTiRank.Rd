% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/21j-TiRankScreen.R
\name{DoTiRank}
\alias{DoTiRank}
\title{Perform TiRank Screening Analysis}
\usage{
DoTiRank(
  matched_bulk,
  sc_data,
  phenotype,
  label_type = "TiRank",
  phenotype_class = c("binary", "survival", "continuous"),
  tirank_params = list(validation_proportion = 0.15, sampling_thresh = 0.5, sampling_mode
    = c("smote", "downsample", "upsample", "tomeklinks"), top_var_genes = 2000L,
    top_gene_pairs = 1000L, p_value_threshold = 0.05, max_cutoff = 0.8, min_cutoff =
    -0.8, nhead = 2L, nhid1 = 96L, nhid2 = 8L, n_output = 32L, nlayers = 3L, n_pred = 2L,
    dropout = 0.5, encoder_type = c("MLP", "Transformer", "DenseNet"), infer_mode =
    c("SC", "ST"), n_trials = 5L, do_reject = TRUE, tolerance = 0.05),
  save_path = "./TiRank_res",
  load_cache = NULL,
  ...
)
}
\arguments{
\item{matched_bulk}{Matrix or data frame of preprocessed bulk RNA-seq expression
data (genes x samples). Column names must match names/IDs in \code{phenotype}.}

\item{sc_data}{A matrix/Matrix (genes x cells) or a Seurat object containing
scRNA-seq data to be screened.}

\item{phenotype}{Phenotype data, either:
\itemize{
\item Named vector (names match \code{matched_bulk} columns), or
\item Patient survival data frame with row names matching \code{matched_bulk}
columns, colnames named "time" and "status"
}}

\item{label_type}{Character specifying phenotype label type (default: \code{"TiRank"}).}

\item{phenotype_class}{Type of phenotypic outcome (must be consistent with input data):
\itemize{
\item \code{"binary"}: Binary traits (e.g., case/control)
\item \code{"continuous"}: Continuous measurements
\item \code{"survival"}: Survival information
}}

\item{tirank_params}{List of TiRank algorithm parameters:

\strong{Data preprocessing:}
\itemize{
\item \code{validation_proportion}: Proportion of bulk data held out for validation (default: \code{0.15}).
\item \code{sampling_thresh}: Threshold for resampling strategy (default: \code{0.5}).
\item \code{sampling_mode}: Resampling method: \code{"smote"}, \code{"downsample"}, \code{"upsample"}, or \code{"tomeklinks"}.
\item \code{top_var_genes}: Number of top variable genes to select (default: \code{2000L}).
\item \code{top_gene_pairs}: Number of top gene pairs for ranking (default: \code{1000L}).
\item \code{p_value_threshold}: P-value threshold for feature selection (default: \code{0.05}).
\item \code{max_cutoff}: Upper cutoff for correlation filtering (default: \code{0.8}).
\item \code{min_cutoff}: Lower cutoff for correlation filtering (default: \code{-0.8}).
}

\strong{Neural network architecture:}
\itemize{
\item \code{nhead}: Number of attention heads (default: \code{2L}).
\item \code{nhid1}: Size of first hidden layer (default: \code{96L}).
\item \code{nhid2}: Size of second hidden layer (default: \code{8L}).
\item \code{n_output}: Output dimension of encoder (default: \code{32L}).
\item \code{nlayers}: Number of encoder layers (default: \code{3L}).
\item \code{n_pred}: Number of prediction heads (default: \code{2L}).
\item \code{dropout}: Dropout rate for regularization (default: \code{0.5}).
\item \code{encoder_type}: Encoder architecture: \code{"MLP"}, \code{"Transformer"}, or \code{"DenseNet"}.
\item \code{infer_mode}: Inference mode: \code{"SC"} (single-cell) or \code{"ST"} (spatial transcriptomics).
}

\strong{Training:}
\itemize{
\item \code{n_trials}: Number of repeated training trials (default: \code{5L}).
\item \code{do_reject}: Whether to apply rejection criteria to uncertain predictions (default: \code{TRUE}).
\item \code{tolerance}: Tolerance threshold for rejection (default: \code{0.05}).
}}

\item{save_path}{(Soft-deprecated) Directory path for saving intermediate and
final results (default: \code{"./TiRank_res"}). Acts as fallback for
\code{load_cache} and \code{save_cache} when not specified via \code{...}.
Prefer using \code{load_cache} / \code{save_cache} in \code{...} instead.
See \code{\link[=CacheSetHere]{CacheSetHere()}}.}

\item{load_cache}{(Soft-deprecated) Optional path to cached data (default: \code{NULL}).
Prefer using \code{load_cache} in \code{...} instead.}

\item{...}{Additional arguments passed to the function. Common parameters include:
\itemize{
\item \code{verbose}: Logical. Whether to print verbose output (default: TRUE).
\item \code{seed}: Integer. Random seed for reproducibility.
\item \code{assay}: Character. Name of assay to use from Seurat object (default: \code{"RNA"}).
\item \code{load_cache}: Cache directory path for loading cached data. Supports
root-level, cache-level, or parent-level paths. See \code{\link[=CacheSetHere]{CacheSetHere()}}.
\item \code{save_cache}: Cache directory path for saving results. Supports
root-level or parent-level paths. See \code{\link[=CacheSetHere]{CacheSetHere()}}.
}}
}
\value{
A named list containing:
\itemize{
\item \strong{scRNA_data}: Modified single-cell data object with integrated screening
results added as metadata, including \code{TiRank_Reject}, \code{TiRank_Rank_Score},
and \code{TiRank} (Positive/Neutral/Negative) columns, plus \code{TiRank_para}
and \code{TiRank_type} stored in misc slot.
\item \strong{cell_cell_distance}: Computed cell-cell similarity/distance matrix.
}
}
\description{
Integrates single-cell and bulk RNA-seq data using the TiRank deep learning
framework to identify phenotype-associated cells. TiRank employs a
rank-based approach with neural network models to score and classify cells
based on their association with the phenotype of interest.
}
\details{
The TiRank screening workflow consists of the following steps:
\enumerate{
\item \strong{Data preprocessing:} Normalizes bulk expression data and validates
compatibility with phenotype information.
\item \strong{Expression transfer:} Transfers the single-cell expression profile
into the TiRank-compatible format.
\item \strong{Validation set generation:} Splits bulk data into training and
validation sets according to \code{validation_proportion}.
\item \strong{Resampling:} Applies the specified sampling strategy to address
class imbalance in bulk data.
\item \strong{Cell-cell similarity:} Computes cell-cell distance or similarity
matrix from single-cell data.
\item \strong{Model training:} Trains a TiRank neural network (MLP, Transformer,
or DenseNet encoder) using bulk expression and phenotype.
\item \strong{Screening:} Applies the trained model to score each cell, producing
rank-based predictions with rejection handling.
\item \strong{Label assignment:} Classifies cells as \code{"Positive"},
\code{"Neutral"}, or \code{"Negative"} based on rank scores.
}
}
\examples{
\dontrun{
# Binary classification example
result <- DoTiRank(
  matched_bulk = bulk_matrix,
  sc_data = seurat_obj,
  phenotype = bulk_phenotype,
  phenotype_class = "binary",
  save_path = "./TiRank_res"
)

# Survival analysis example
result <- DoTiRank(
  matched_bulk = bulk_matrix,
  sc_data = seurat_obj,
  phenotype = survival_data,
  phenotype_class = "survival",
  tirank_params = list(
    encoder_type = "Transformer",
    n_trials = 3L
  ),
  save_path = "./TiRank_survival_res"
)

# Access results
modified_seurat <- result$scRNA_data
head(modified_seurat[[]])
}

}
\seealso{
Other TiRank:
\code{\link[=ValidateTiRankParams]{ValidateTiRankParams()}}

Other screen_method:
\code{\link[=DoDEGAS]{DoDEGAS()}},
\code{\link[=DoLP_SGL]{DoLP_SGL()}},
\code{\link[=DoPIPET]{DoPIPET()}},
\code{\link[=DoSCIPAC]{DoSCIPAC()}},
\code{\link[=DoSIDISH]{DoSIDISH()}},
\code{\link[=DoScissor]{DoScissor()}},
\code{\link[=DoscAB]{DoscAB()}},
\code{\link[=DoscPAS]{DoscPAS()}},
\code{\link[=DoscPP]{DoscPP()}}
}
\concept{TiRank}
\concept{screen_method}

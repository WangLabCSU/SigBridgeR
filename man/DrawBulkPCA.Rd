% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-DrawBulkPCA.R
\name{DrawBulkPCA}
\alias{DrawBulkPCA}
\title{Draw PCA Plot for Bulk Expression Data}
\usage{
DrawBulkPCA(bulk, group, batch = NULL, show_plot = TRUE, ...)
}
\arguments{
\item{bulk}{A numeric matrix or data frame of bulk expression data
(genes x samples). Column names are used as sample identifiers.}

\item{group}{A vector of group labels, one per sample (column of \code{bulk}).
Used for coloring points and ellipses.}

\item{batch}{An optional vector of batch labels, one per sample. When
provided, points are additionally shaped by batch (up to 4 batches
supported). Default: \code{NULL}.}

\item{show_plot}{Logical. If \code{TRUE} (default), the plot is printed
immediately.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the combined \code{patchwork}/\code{ggplot} object.
}
\description{
Performs PCA on transposed bulk expression data and visualizes the first two
principal components as a scatter plot with marginal density plots. Optionally
overlays batch information as point shapes (up to 4 batches).

The plot is composed via \link[patchwork:patchwork]{patchwork} from:
\itemize{
\item A top marginal density plot for PC1.
\item A right marginal density plot for PC2.
\item A central scatter plot with group-colored ellipses (via \code{\link[ggforce:geom_mark_ellipse]{ggforce::geom_mark_ellipse()}}).
}
}
\examples{
\dontrun{
# Small example matrix: 10 genes x 8 samples
set.seed(123)
bulk <- matrix(
  rnorm(100 * 80, mean = 10, sd = 2),
  nrow = 100, ncol = 80,
  dimnames = list(
    paste0("Gene", 1:100),
    paste0("Sample", 1:80)
  )
)

group <- rep(c("Control", "Treatment"), each = 40)

# Basic PCA plot without batch
DrawBulkPCA(bulk = bulk, group = group)

# PCA plot with batch shapes
batch <- rep(c("Batch1", "Batch2"), times = 40)
DrawBulkPCA(bulk = bulk, group = group, batch = batch)
}

}
\seealso{
Other input_preprocess:
\code{\link[=PhenoMap]{PhenoMap()}},
\code{\link[=PhenoPreProcess]{PhenoPreProcess()}},
\code{\link[=SCPreProcess]{SCPreProcess()}},
\code{\link[=SymbolConvert]{SymbolConvert()}}
}
\concept{input_preprocess}

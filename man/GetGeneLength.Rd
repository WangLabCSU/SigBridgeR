% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12a-GetGeneLength.R
\name{GetGeneLength}
\alias{GetGeneLength}
\alias{GetGeneLength.default}
\alias{GetGeneLength.character}
\title{Compute Gene Lengths from a GTF File or GTF Lines}
\usage{
GetGeneLength(x, verbose = TRUE, ...)

\method{GetGeneLength}{default}(x, verbose = TRUE, ...)

\method{GetGeneLength}{character}(x, verbose = TRUE, ...)
}
\arguments{
\item{x}{The object to dispatch on:
\itemize{
\item A length-1 character string: path to a GTF or GTF.GZ file.
\item A character vector with more than one element: the GTF lines.
}}

\item{verbose}{A logical flag. If \code{TRUE} (default), cli-style progress
messages (with ANSI colors) are printed while reading, parsing, and
merging the exon intervals.}

\item{...}{Reserved for future use.}
}
\value{
A named numeric vector of gene lengths, one entry per gene, with
the gene ids as names. An empty (zero-length) named vector is returned
when the input contains no valid \code{exon} records.
}
\description{
An S3 generic that computes per-gene lengths by parsing \code{exon} features
from either a GTF/GTF.GZ file or a character vector of GTF lines. The
reported length for each gene is the total merged (non-overlapping) span
of its exons.

The \code{character} method accepts two forms of input:
\itemize{
\item A \strong{single-element} character vector \code{x} is treated as a GTF file path
(optionally gzip-compressed). The file is streamed line-by-line and is
never loaded fully into memory.
\item A \strong{multi-element} character vector \code{x} is treated as the GTF lines
themselves (e.g. the output of \code{\link[=readLines]{readLines()}}).
}

Only \code{exon} features located on autosomes \code{1-22} or chromosomes \code{X}, \code{Y},
and \code{MT} are considered. Overlapping or adjacent exon intervals are merged
within each (\code{gene_id}, chromosome, strand) group, and the length of the
merged span is reported. Ensembl \code{gene_id} version suffixes (e.g. \code{.15})
are stripped so identifiers from different Ensembl builds map together.
}
\examples{
\dontrun{
# Gene lengths from a gzip-compressed GTF file
lengths <- GetGeneLength("Homo_sapiens.GRCh37.75.gtf.gz")

# Gene lengths from GTF lines already read into R
x <- readLines("Homo_sapiens.GRCh37.75.gtf.gz")
lengths <- GetGeneLength(x)
}

}

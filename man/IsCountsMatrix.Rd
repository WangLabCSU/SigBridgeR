% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13d-IsCountsMatrix.R
\name{IsCountsMatrix}
\alias{IsCountsMatrix}
\title{Check whether a matrix looks like RNA-seq raw counts}
\usage{
IsCountsMatrix(
  x,
  verbose = TRUE,
  integer_tol = 1e-08,
  min_integer_fraction = 0.95
)
}
\arguments{
\item{x}{Numeric matrix. Rows are genes, columns are samples.}

\item{verbose}{Logical. Whether to print diagnostic statistics (fractions of
non-negative, integer-like, and zero values, library sizes, and the reason
for rejection) to the console. Default: \code{TRUE}.}

\item{integer_tol}{Numeric in \verb{[0, 1]}. Absolute tolerance used to decide
whether a value is integer-like (\code{abs(x - round(x)) <= integer_tol}).
Default: \code{1e-8}.}

\item{min_integer_fraction}{Numeric in \verb{[0, 1]}. Minimum fraction of
integer-like values required for the matrix to be considered counts.
Default: \code{0.95}.}
}
\value{
A single logical value: \code{TRUE} if \code{x} is likely a bulk counts
matrix, \code{FALSE} otherwise.
}
\description{
Determines whether a numeric matrix is likely to be a bulk/single-cell RNA-seq raw
counts matrix. A matrix is accepted only if it is non-empty, finite,
non-negative, integer-like in a sufficiently high proportion, and has a
positive library size for every sample. Useful for validating input before
running count-based downstream analyses.
}
\details{
\code{IsCountsMatrix()} rejects a matrix for any of the following reasons:
\itemize{
\item the matrix is empty;
\item the matrix contains \code{NA}, \code{NaN}, or infinite values;
\item the matrix contains negative values;
\item the fraction of integer-like values is below \code{min_integer_fraction};
\item at least one sample (column) has a library size (column sum) \verb{<= 0}.
}

When \code{verbose = TRUE}, the reason for rejection (or diagnostic statistics on
acceptance) is printed to the console.
}
\examples{
# A typical bulk RNA-seq raw counts matrix (genes x samples)
counts <- matrix(
  c(100, 200, 300, 150, 250, 350),
  nrow = 3, ncol = 2,
  dimnames = list(c("GENE1", "GENE2", "GENE3"), c("Sample1", "Sample2"))
)
IsCountsMatrix(counts)

# A log-normalized matrix is not counts
IsCountsMatrix(log1p(counts))

# Matrices containing negative values are not counts
IsCountsMatrix(counts - 1000)

# Inspect diagnostic statistics
IsCountsMatrix(counts, verbose = TRUE)

}
\concept{bulk_preprocessing}

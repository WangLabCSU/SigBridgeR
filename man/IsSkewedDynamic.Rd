% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/94-IsSkewedDynamic.R
\name{IsSkewedDynamic}
\alias{IsSkewedDynamic}
\title{Test Whether the Proportion of a Target Value Is Highly Skewed}
\usage{
IsSkewedDynamic(x, target = 0, expected_p = 0.8, n_sd = 4L)
}
\arguments{
\item{x}{A vector to be tested. Must be numeric, integer, or logical.
Missing values are not allowed.}

\item{target}{A single numeric value indicating the target/background value
whose proportion should be evaluated. Default is \code{0}.}

\item{expected_p}{A single numeric value between 0 and 1 indicating the
expected proportion of \code{target} in \code{x}. Default is \code{0.8}.}

\item{n_sd}{A single integer or numeric value specifying how many standard
deviations away from \code{expected_p} are allowed before the vector is
considered skewed. Default is \code{4L}.}
}
\value{
A logical vector of length 1:
\itemize{
\item \code{TRUE} if the observed proportion of \code{target} deviates from \code{expected_p}
by more than \code{n_sd} standard deviations.
\item \code{FALSE} otherwise.
\item \code{NA} if \code{x} has length zero.
}
}
\description{
\code{IsSkewedDynamic()} tests whether the observed proportion of a specified
target/background value in a vector deviates substantially from an expected
proportion.

The function is designed for detecting highly skewed phenotype-like vectors,
especially binary or discrete vectors where one value represents the
background/control class. If the observed proportion of \code{target} differs from
\code{expected_p} by more than \code{n_sd} standard deviations under a binomial model,
the vector is considered skewed.

Formally, let:

\deqn{\hat{p} = \frac{\#(x_i = target)}{n}}

and

\deqn{SD = \sqrt{\frac{expected\_p(1 - expected\_p)}{n}}}

The function returns \code{TRUE} when:

\deqn{|\hat{p} - expected\_p| > n\_sd \times SD}

Otherwise, it returns \code{FALSE}.

If \code{x} has length zero, the function returns \code{NA}.
}
\details{
This function assumes the expected occurrence of \code{target} follows a binomial
proportion model with probability \code{expected_p}. The standard deviation is
computed as:

\deqn{\sqrt{expected\_p(1 - expected\_p) / n}}

where \code{n} is the length of \code{x}.

The check is symmetric: both an unexpectedly high and unexpectedly low
proportion of \code{target} are considered skewed.

Internally, the computation is delegated to a C++ implementation via
Rcpp for efficiency.
}
\section{Missing values}{

\code{x} must not contain missing values. If any \code{NA} is present, the function
throws an error.
}

\section{Supported input types}{

\code{x} may be one of:
\itemize{
\item numeric
\item integer
\item logical
}

Other input types are rejected by the underlying C++ implementation.
}

\examples{
x1 <- c(rep(0, 80), rep(1, 20))
IsSkewedDynamic(x1)

x2 <- c(rep(0, 95), rep(1, 5))
IsSkewedDynamic(x2)

x3 <- c(rep(0, 50), rep(1, 50))
IsSkewedDynamic(x3, expected_p = 0.8)

IsSkewedDynamic(logical(c(rep(FALSE, 80), rep(TRUE, 20))),
                  target = 0,
                  expected_p = 0.8)

IsSkewedDynamic(numeric(0))

}

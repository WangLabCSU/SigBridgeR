% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99-deprecated_RegisterSeuratMethod.R
\name{RegisterSeuratMethod}
\alias{RegisterSeuratMethod}
\title{Register a Seurat Processing Strategy}
\usage{
RegisterSeuratMethod(
  ...,
  overwrite = FALSE,
  registry = SCPreProcessStrategy,
  verbose = getFuncOption("verbose")
)
}
\arguments{
\item{...}{Named arguments where each name is a \strong{strategy key} (e.g., \code{"h"})
and each value is either:
\itemize{
\item A \strong{function}, or
\item A \strong{character string} specifying a function (e.g., \code{"Seurat::RunHarmony"} or \code{"stats::prcomp"}).
}
The function will be automatically wrapped to accept \code{object} and \code{params}, making it compatible with \code{SCPreProcess()} pipelines.}

\item{overwrite}{Logical. If \code{FALSE} (default), throws an error when attempting
to replace an existing strategy. Set to \code{TRUE} to allow updates.}

\item{registry}{Environment. The target registry to store strategies.
Default: \code{SCPreProcessStrategy}.}

\item{verbose}{Logical. Whether to print registration messages.
Default: inherits from \code{getOption("SigBridgeR.verbose")}.}
}
\value{
Invisibly returns \code{TRUE} on successful registration.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Dynamically registers one or more new preprocessing strategies into a strategy
environment (i.e., \code{SCPreProcessStrategy}). Each strategy is stored under
a user-defined key and wraps a Seurat-compatible function to conform to the
standard interface: \code{function(object, params)}.

Supports both direct function objects and character specifications of
package-qualified function names (e.g., \code{h = "Seurat::RunHarmony"}).
This enables safe, lazy resolution of external dependencies and simplifies
pipeline configuration via strings.
}
\examples{
\dontrun{
# Register using a package-qualified name
RegisterSeuratMethod(
  h = "Seurat::RunHarmony",
  registry = SCPreProcessStrategy
)

# Attempting to re-register without `overwrite = TRUE` will fail
# RegisterSeuratMethod(h = Seurat::RunHarmony)  # Error!
}

}
\seealso{
Other Registering: 
\code{\link{RegisterAnnoMethod}()},
\code{\link{RegisterScreenMethod}()}

Other single_cell_preprocess: 
\code{\link{FilterTumorCell}()},
\code{\link{FindRobustElbow}()},
\code{\link{Pattern2Colname}()},
\code{\link{QCPatternDetect}()},
\code{\link{SCAnnotate}()},
\code{\link{SCIntegrate}()},
\code{\link{SCPreProcess}()},
\code{\link{SCPreProcessStrategy}}
}
\concept{Registering}
\concept{single_cell_preprocess}

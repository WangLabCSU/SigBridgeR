% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-PreProcess.R
\name{SCPreProcess}
\alias{SCPreProcess}
\title{Preprocess single-cell RNA data}
\usage{
SCPreProcess(
  sc,
  project = "Scissor_Single_Cell",
  min_cells = 400,
  min_features = 0,
  normalization_method = "LogNormalize",
  scale_factor = 10000,
  selection_method = "vst",
  resolution = 0.6,
  dims = 1:10,
  verbose = TRUE,
  future_global_maxsize = 6 * 1024^3
)
}
\arguments{
\item{sc}{raw scRNA data read from \code{anndata::read.h5ad}, contains \verb{$X} matrix and \verb{$obs} metadata}

\item{project}{The project name of the Seurat object, transfered to \code{SeuratObject@project}.}

\item{min_cells}{The minimum number of cells that a feature must be observed in to be included in \code{VariableFeatures}.}

\item{min_features}{The minimum number of cells that a feature must be expressed in to be included in \code{VariableFeatures}.}

\item{normalization_method}{The method used to normalize the data. Options are \code{"LogNormalize"} (default), \code{"CLR"}, \code{"RC"} or \code{"RCLogNormalize"}.}

\item{scale_factor}{A numeric value by which the data will be multiplied. Default is \code{10000}.}

\item{selection_method}{The method used to select variable features, transferred to \code{SeuratObject@assays$RNA@var.features}. Options are \code{"vst"} (default), \code{"mean.var.plot"}, \code{"dispersion"}, \code{"mean.var.plot"} or \code{"dispersion.scaled"}.}

\item{resolution}{The resolution parameter for clustering. Default is \code{0.6}.}

\item{dims}{The dimensions to use for clustering. Default is \code{1:10}.}

\item{verbose}{Whether to show detailed information. Default is \code{TRUE}.}

\item{future_global_maxsize}{The memory allocated.}
}
\value{
A Seurat object
}
\description{
Preprocess single-cell RNA data: Create a Seurat object, normalize data, find variable features, scale data, run PCA, find clusters and neighbors, run tSNE and UMAP.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06-S3_base.R
\name{SigBridgeR-S3}
\alias{SigBridgeR-S3}
\alias{print.SigBridgeR::SigBridgeRBase}
\alias{summary.SigBridgeR::SigBridgeRBase}
\alias{length.SigBridgeR::SigBridgeRBase}
\alias{names.SigBridgeR::SigBridgeRBase}
\alias{names<-.SigBridgeR::SigBridgeRBase}
\alias{format.SigBridgeR::SigBridgeRBase}
\alias{as.list.SigBridgeR::SigBridgeRBase}
\alias{as.character.SigBridgeR::SigBridgeRBase}
\title{Base R Methods for SigBridgeR Objects}
\usage{
\method{print}{`SigBridgeR::SigBridgeRBase`}(x, ...)

\method{summary}{`SigBridgeR::SigBridgeRBase`}(object, ...)

\method{length}{`SigBridgeR::SigBridgeRBase`}(x)

\method{names}{`SigBridgeR::SigBridgeRBase`}(x)

\method{names}{`SigBridgeR::SigBridgeRBase`}(x) <- value

\method{format}{`SigBridgeR::SigBridgeRBase`}(x, ...)

\method{as.list}{`SigBridgeR::SigBridgeRBase`}(x, ...)

\method{as.character}{`SigBridgeR::SigBridgeRBase`}(x, ...)
}
\arguments{
\item{x}{A \code{SigBridgeRBase} object. Basically all SigBridgeR objects.}

\item{...}{Additional arguments passed to the method. Currently unused.}

\item{object}{A \code{SigBridgeRBase} object. Basically all SigBridgeR objects.}

\item{value}{A character vector of property names.}
}
\value{
Invisibly returns \code{x}.

Invisibly returns \code{object}.

An integer: the number of properties.

A character vector of property names.

A character vector of property names.

A character vector.

A named list of properties.

Throws an error; never returns.
}
\description{
S3 methods that make every \code{SigBridgeRBase} object -- and therefore every
SigBridgeR object, such as \code{ScreenMethod}, \code{ScreenMethodResult}, and
\code{InterceptStrategy} -- behave like a standard R object. They support the
base generics \code{print()}, \code{length()}, \code{names()}, \verb{names<-()}, \code{format()},
\code{as.list()}, and \code{as.character()}.

Pretty-print a \code{SigBridgeR} object using the \code{cli} package.

The class name is displayed in bold blue. Slots are displayed as an
unordered list, with slot names in regular blue and each value preceded by
its type.

Returns the number of properties (slots) stored in a \code{SigBridgeRBase} object.

Returns the names of all properties (slots) stored in a \code{SigBridgeRBase}
object.

Returns the names of all properties (slots) stored in a \code{SigBridgeRBase}
object.

Format a \code{SigBridgeRBase} object as a compact character vector.

Converts a \code{SigBridgeRBase} object into a named list of its properties.

Coercion of \code{SigBridgeRBase} to character is intentionally unsupported.
Use \code{\link[=format]{format()}} to obtain a character representation instead.
}
\details{
All SigBridgeR objects inherit from the abstract base class
\link{SigBridgeRBase} and store their data in S7 properties. The methods
documented on this page expose those properties through familiar base R
interfaces:
\itemize{
\item \code{print()} displays a human-readable summary of the object. The class
name is shown in bold blue and each property is listed with its name,
value type, and value.
\item \code{length()} returns the number of properties stored in the object.
\item \code{names()} returns the names of the object's properties.
\item \verb{names<-()} is restricted: properties are validated by their S7
definitions, so modifying them in place is not allowed. Reconstruct a
new object instead.
\item \code{format()} converts the object to a compact character vector, starting
with a header of the form \verb{<Class: SigBridgeR <version>>}.
\item \code{as.list()} converts the object to a named list of its properties.
}

\code{as.character()} is intentionally unsupported: coercing a
\code{SigBridgeRBase} object to a character vector is not meaningful and
always throws an error. Use \code{\link[=format]{format()}} instead.

The class name is displayed in bold blue. Slots are displayed as an
unordered list, with slot names in regular blue and each value preceded by
its type.

The first line is displayed as \verb{<Class: name version>} when the object has
\code{method_name} and \code{method_version} properties, for example
\verb{<ScreenMethod: SigBridgeR 4.0.0>}. Each following line contains one property name
and its value on the same line. Property types are not shown.
}
\seealso{
\itemize{
\item \link{SigBridgeRBase} for the abstract base class on which these methods
dispatch.
\item \code{\link[S7:props]{S7::props()}} for the underlying property accessor.
}
}

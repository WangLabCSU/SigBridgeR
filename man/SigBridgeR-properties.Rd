% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04a-ClassBase.R
\docType{data}
\name{SigBridgeR-properties}
\alias{SigBridgeR-properties}
\alias{property_sigbridger_verison}
\alias{property_verison}
\alias{property_chr}
\alias{property_phenotype_class}
\alias{property_fn}
\alias{property_list}
\alias{property_mapper_fn}
\alias{property_py_env}
\alias{property_data_list}
\alias{property_seurat}
\title{SigBridgeR Base Class Properties}
\format{
An S7 property of class \code{class_character}.

An S7 property of class \code{class_character}.

An S7 property of class \code{class_character}.

An S7 property of class \code{class_character}.

An S7 property of class \code{class_function}.

An S7 property of class \code{class_list}.

An S7 property of class \code{class_any}.

An S7 property of class \code{class_any}.

An S7 property of class \code{class_list}.

An S7 property of class \code{class_any}.
}
\usage{
property_sigbridger_verison

property_verison

property_chr

property_phenotype_class

property_fn

property_list

property_mapper_fn

property_py_env

property_data_list

property_seurat
}
\description{
These are S7 property definitions used within the \code{SigBridgeRBase} class
and its subclasses. They define the types, validators, and default values
for class attributes. These properties are exported for community developers
to extend and subclass \code{SigBridgeRBase}.
}
\details{
A read-only character property that returns the current version of the
\code{SigBridgeR} package. Attempting to set this property will raise an error.

The version string is validated to contain at least one dot (\code{.}).
The default value is obtained from \code{get_pkg_version()}.

A read-only character property that returns the current version of the
\code{SigBridgeR} package. Attempting to set this property will raise an error.

The version string is validated to contain at least one dot (\code{.}).
The default value is obtained from \code{get_pkg_version()}.

A general-purpose character property with no additional validation.
Can be used for any string-typed attribute in subclasses.

A character property that specifies the phenotype classification type(s).
Valid values are \code{"binary"}, \code{"continuous"}, and \code{"survival"}.
Multiple values can be provided as a character vector.

Default: \code{c("binary", "continuous", "survival")}.

A property that accepts a function value. Used to store callback or
transformation functions in class attributes.

A named list property. The value must be a named list (not \code{NULL} or
\code{NA}) with unique names. Duplicate names are not allowed.

A property that accepts either a function or \code{NULL}. Used for storing
mapper/transformation functions that may be optionally specified.

If a value is provided, it must be a function; otherwise, \code{NULL} is
accepted to indicate no mapper is configured.

A property representing a Python environment configuration.
Accepts either a character string (e.g., a conda environment name or path)
or \code{NULL} (when no Python environment is specified).

A named list property for storing multiple data objects.
The list must be named (if not \code{NULL}) and names must be unique.

Unlike \code{property_list}, this property allows \code{NULL}, \code{NA}, and empty
lists (\code{list()}) as valid values, making it suitable for optional
data storage.

A property that requires a \link[SeuratObject:Seurat-class]{Seurat} object.
The value must inherit from the \code{"Seurat"} class.

This property is used to store single-cell data in Seurat format
for downstream analysis.
}
\examples{
# Valid usage
property_phenotype_class@validator("binary")
property_phenotype_class@validator(c("binary", "survival"))
}
\seealso{
\link[SeuratObject:Seurat-class]{SeuratObject::Seurat}
}
